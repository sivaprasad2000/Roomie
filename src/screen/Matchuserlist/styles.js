import { View,
    Text,
    StyleSheet,
    Dimensions,
    } from 'react-native'

const styles = StyleSheet.create({
    container:{
        flex:1,
        backgroundColor: '#e15f8b',
    },
    headwrap:{
        paddingTop:80,
        paddingHorizontal:20,

    },
    welcomeText: {
        fontSize: 30,
        fontWeight:'bold'
    },
});

export default styles;
